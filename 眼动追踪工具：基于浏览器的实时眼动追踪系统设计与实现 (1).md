# 眼动追踪工具：基于浏览器的实时眼动追踪系统设计与实现

**作者**: Manus AI  
**日期**: 2026年2月  
**版本**: 1.0

---

## 摘要

本文介绍了一个基于现代网络技术的眼动追踪工具，该工具利用计算机内置的前置摄像头实时追踪用户的眼动轨迹。该系统采用 WebGazer.js 库作为核心追踪引擎，结合 React 框架和 HTML5 Canvas API 实现了完整的眼动数据采集、校准和可视化功能。系统包含两个主要模块：图像显示模块用于全屏展示待分析内容，追踪模块在后台运行以采集眼动数据。通过热力图和视线轨迹图两种可视化方式，用户可以获得关于阅读顺序和注意力分布的详细数据报告。本文详细阐述了系统的架构设计、核心技术实现、数据处理流程以及性能优化方案。

**关键词**: 眼动追踪，WebGazer，浏览器端追踪，数据可视化，校准算法

---

## 1. 引言

### 1.1 研究背景

眼动追踪技术是一种通过测量和记录用户眼睛位置和运动来理解人类视觉行为的方法。传统的眼动追踪系统通常需要专门的硬件设备和实验室环境，这限制了其在实际应用中的普及[1]。随着网络技术的发展，基于浏览器的眼动追踪方案应运而生，使得任何具有网络摄像头的计算机都可以进行眼动追踪实验[2]。

眼动追踪在多个领域具有重要应用价值。在用户界面研究中，眼动数据可以揭示用户对界面元素的关注模式[3]。在教育研究中，眼动追踪可以帮助理解学生的学习过程和认知负荷[4]。在可用性测试中，眼动数据提供了关于用户交互体验的客观量化指标[5]。

### 1.2 现有技术分析

WebGazer.js 是一个开源的浏览器端眼动追踪库，由 Brown 大学的研究团队开发[2]。该库通过以下方式实现眼动追踪：首先使用计算机视觉技术从网络摄像头捕获的视频流中检测眼睛特征，然后通过机器学习模型将眼睛特征映射到屏幕上的凝视位置。WebGazer.js 的创新之处在于它采用了基于用户交互的自校准方法，用户无需进行复杂的初始设置即可开始使用。

### 1.3 本工具的创新点

本眼动追踪工具在 WebGazer.js 的基础上进行了以下创新和优化：

1. **简化的校准流程**：采用 9 点校准布局（3×3 网格），每个点需要 3 次点击，相比传统的 12 点或更多点的校准方案大幅降低了用户操作负担。

2. **实时视线反馈**：在校准和测试过程中显示实时的视线光标，用户可以直观地看到追踪效果，增强了系统的可用性和用户体验。

3. **双重可视化方案**：提供热力图和视线轨迹图两种互补的数据可视化方式，热力图展示注意力分布的密度，视线轨迹图展示眼动的时序过程。

4. **原生 Canvas 实现**：使用 HTML5 Canvas API 替代传统的 heatmap.js 库，避免了库的兼容性问题，同时提高了性能。

5. **隐私保护设计**：所有数据处理均在浏览器本地进行，摄像头数据不上传到任何服务器，充分保护用户隐私。

---

## 2. 系统架构

### 2.1 总体架构

本眼动追踪工具采用前端单页应用（SPA）架构，所有处理均在客户端浏览器中进行。系统的总体架构如下图所示：

```
┌─────────────────────────────────────────────────┐
│         用户界面层 (React Components)            │
├─────────────────────────────────────────────────┤
│  Home │ Calibration │ ImageViewer │ Report      │
├─────────────────────────────────────────────────┤
│         业务逻辑层 (React Hooks & State)         │
├─────────────────────────────────────────────────┤
│  useWebGazer Hook │ 数据管理 │ 状态管理         │
├─────────────────────────────────────────────────┤
│         核心追踪层 (WebGazer.js)                 │
├─────────────────────────────────────────────────┤
│  眼睛检测 │ 凝视估计 │ 校准管理 │ 数据采集      │
├─────────────────────────────────────────────────┤
│         硬件接口层 (Browser APIs)               │
├─────────────────────────────────────────────────┤
│  MediaDevices API │ Canvas API │ Storage API    │
└─────────────────────────────────────────────────┘
```

系统分为以下几个主要模块：

| 模块名称 | 功能描述 | 关键组件 |
|---------|--------|--------|
| 用户界面模块 | 提供用户交互界面 | Home, Calibration, ImageViewer, Report |
| 图像管理模块 | 处理图像上传和显示 | react-dropzone, ImageViewer |
| 眼动追踪模块 | 实现眼动追踪功能 | useWebGazer Hook, WebGazer.js |
| 数据可视化模块 | 生成热力图和视线轨迹图 | Heatmap, GazePlot, Canvas API |
| 报告生成模块 | 生成分析报告 | Report, 数据统计 |

### 2.2 技术栈

系统采用以下技术栈：

| 层级 | 技术 | 版本 | 用途 |
|-----|------|------|------|
| 前端框架 | React | 19.2.1 | UI 组件和状态管理 |
| 构建工具 | Vite | 7.1.7 | 快速开发和生产构建 |
| 样式框架 | Tailwind CSS | 4.1.14 | 响应式设计和主题 |
| UI 组件库 | shadcn/ui | - | 现代化 UI 组件 |
| 眼动追踪 | WebGazer.js | 3.4.0 | 浏览器端眼动追踪 |
| 文件上传 | react-dropzone | 14.3.8 | 拖拽上传功能 |
| 路由管理 | Wouter | 3.3.5 | 轻量级路由 |
| 图标库 | lucide-react | 0.453.0 | 矢量图标 |

---

## 3. 核心技术实现

### 3.1 WebGazer.js 眼动追踪原理

#### 3.1.1 基本工作流程

WebGazer.js 的眼动追踪过程包含以下步骤：

**步骤 1：眼睛特征检测**  
WebGazer.js 使用计算机视觉库（如 clmtrackr）从网络摄像头的视频流中检测眼睛的关键特征点，包括瞳孔位置、眼睛边界等。这一步骤在每一帧视频上执行，通常以 30fps 的频率进行。

**步骤 2：凝视点估计**  
基于检测到的眼睛特征，WebGazer.js 使用机器学习模型（通常是线性回归或岭回归）将眼睛特征映射到屏幕上的凝视坐标。该模型在校准阶段通过用户的已知点击位置进行训练。

**步骤 3：校准**  
WebGazer.js 采用基于用户交互的自校准方法。当用户点击屏幕上的特定位置时，系统记录该位置作为已知的凝视点，并使用该数据点来改进凝视估计模型。多个校准点可以提高模型的准确度。

**步骤 4：数据平滑**  
为了减少原始眼动数据中的噪声和抖动，WebGazer.js 应用卡尔曼滤波器对凝视数据进行平滑处理。这使得追踪的眼动轨迹更加稳定和连贯。

#### 3.1.2 回归模型

WebGazer.js 支持多种回归模型用于凝视估计，本工具采用的是岭回归（Ridge Regression）模型。岭回归通过在损失函数中加入正则化项来防止过拟合：

$$J(w) = \frac{1}{2m}\sum_{i=1}^{m}(h_w(x^{(i)}) - y^{(i)})^2 + \frac{\lambda}{2m}\sum_{j=1}^{n}w_j^2$$

其中，$h_w(x)$ 是预测函数，$y$ 是实际凝视点，$\lambda$ 是正则化参数，$m$ 是训练样本数，$n$ 是特征数。

#### 3.1.3 卡尔曼滤波

卡尔曼滤波是一种递归算法，用于从一系列含有噪声的测量值中估计动态系统的状态。在眼动追踪中，卡尔曼滤波用于平滑凝视坐标的时间序列。

卡尔曼滤波的核心方程为：

**预测步骤**：
$$\hat{x}_{k|k-1} = A\hat{x}_{k-1|k-1}$$
$$P_{k|k-1} = AP_{k-1|k-1}A^T + Q$$

**更新步骤**：
$$K_k = \frac{P_{k|k-1}}{P_{k|k-1} + R}$$
$$\hat{x}_{k|k} = \hat{x}_{k|k-1} + K_k(z_k - \hat{x}_{k|k-1})$$
$$P_{k|k} = (1 - K_k)P_{k|k-1}$$

其中，$\hat{x}$ 是状态估计，$P$ 是估计误差协方差，$Q$ 是过程噪声协方差，$R$ 是测量噪声协方差，$K_k$ 是卡尔曼增益。

### 3.2 自定义 React Hook：useWebGazer

为了在 React 应用中优雅地集成 WebGazer.js，我们开发了一个自定义 Hook `useWebGazer`，该 Hook 封装了 WebGazer.js 的初始化、校准、追踪等核心功能。

#### 3.2.1 Hook 的设计原理

```typescript
export const useWebGazer = () => {
  const [isReady, setIsReady] = useState(false);
  const [isTracking, setIsTracking] = useState(false);
  const isTrackingRef = useRef(false);
  const [gazeData, setGazeData] = useState<GazeData | null>(null);
  const gazeDataRef = useRef<GazeData[]>([]);

  // 初始化 WebGazer
  useEffect(() => {
    const initWebGazer = async () => {
      await webgazer.setRegression('ridge')
        .setGazeListener((data: any, clock: number) => {
          if (data && isTrackingRef.current) {
            gazeDataRef.current.push({ x: data.x, y: data.y, timestamp: clock });
          }
        })
        .saveDataAcrossSessions(true)
        .begin();
      
      webgazer.removeMouseEventListeners();
      webgazer.showVideoPreview(false).showPredictionPoints(false);
      setIsReady(true);
    };
    
    initWebGazer();
    return () => webgazer.end();
  }, []);

  return { isReady, isTracking, gazeData, startTracking, stopTracking, calibratePoint };
};
```

#### 3.2.2 关键设计决策

**1. 使用 Ref 稳定闭包中的状态**

在 `setGazeListener` 回调中，我们需要访问 `isTracking` 状态来判断是否应该记录眼动数据。然而，由于 JavaScript 闭包的特性，直接使用 `isTracking` 状态会导致回调函数使用过时的状态值。解决方案是使用 `useRef` 创建一个可变引用 `isTrackingRef`，并在状态变化时同步更新该引用。

```typescript
useEffect(() => {
  isTrackingRef.current = isTracking;
}, [isTracking]);
```

**2. 禁用默认鼠标校准**

WebGazer.js 默认启用鼠标事件监听，这会导致用户的鼠标移动被记录为校准数据，从而引入"鼠标跟随"偏差。通过调用 `webgazer.removeMouseEventListeners()` 禁用此功能，我们确保只有明确的点击操作才被用作校准点。

**3. 跨会话数据保存**

通过调用 `saveDataAcrossSessions(true)`，WebGazer.js 会将校准数据保存到浏览器的本地存储中。这样，用户在后续访问时无需重新校准，提高了用户体验。

### 3.3 校准系统设计

#### 3.3.1 9 点校准布局

本工具采用 3×3 网格的 9 点校准布局，校准点分布如下：

```
(10%, 10%)    (50%, 10%)    (90%, 10%)
(10%, 50%)    (50%, 50%)    (90%, 50%)
(10%, 90%)    (50%, 90%)    (90%, 90%)
```

这种布局覆盖了屏幕的各个区域，能够有效地校准眼动追踪系统在整个屏幕范围内的准确度。相比传统的 12 点或 5 点校准方案，9 点布局在准确度和用户体验之间取得了良好的平衡。

#### 3.3.2 多次点击校准

每个校准点需要用户点击 3 次。这种多次点击的设计有以下优势：

1. **增加数据样本**：多次点击为同一位置提供了多个数据点，使得回归模型能够更好地学习该位置的眼睛特征。

2. **降低噪声影响**：单次点击可能受到随机噪声的影响，多次点击的平均效果可以减少噪声的影响。

3. **提高模型鲁棒性**：多个样本点使得模型对眼睛特征的变化更加鲁棒，提高了在不同条件下的泛化能力。

#### 3.3.3 实时视线反馈

在校准过程中，系统显示一个实时的视线光标（蓝色圆点），该光标跟随用户的眼睛运动。这个反馈机制有以下作用：

1. **用户确认**：用户可以直观地看到系统是否正确追踪了他们的眼睛。

2. **校准指导**：如果视线光标偏离校准点，用户可以调整头部位置或眼睛方向以获得更好的校准效果。

3. **系统诊断**：如果在校准过程中视线光标表现异常，可能表明摄像头位置不佳或光线条件不理想，用户可以进行相应的调整。

### 3.4 摄像头视频流处理

#### 3.4.1 视频元素的隐藏与定位

WebGazer.js 自动创建一个 `<video>` 元素来捕获摄像头视频流。为了避免在用户界面上显示摄像头预览（这会分散用户的注意力），我们需要隐藏该视频元素，同时保持视频流的处理继续进行。

关键的实现细节如下：

```typescript
const videoElement = document.getElementById('webgazerVideoFeed');
if (videoElement) {
  videoElement.style.display = 'block';        // 必须是 block 或 inline
  videoElement.style.position = 'fixed';
  videoElement.style.top = '-9999px';          // 移出屏幕
  videoElement.style.left = '-9999px';
  videoElement.style.opacity = '0';            // 透明度为 0
  videoElement.style.pointerEvents = 'none';   // 不响应鼠标事件
  videoElement.style.zIndex = '-1';            // 放在最后面
}
```

**为什么不能使用 `display: none`？**

初始开发时，我们尝试使用 `display: none` 来隐藏视频元素。然而，这导致了一个严重的问题：在某些浏览器中，当视频元素的 `display` 属性被设置为 `none` 时，浏览器会停止处理视频流，导致眼动追踪完全失效。这是因为浏览器的优化机制会停止处理不可见元素的媒体流。

解决方案是保持视频元素的 `display` 属性为 `block` 或 `inline`，但通过其他 CSS 属性（如 `position: fixed` 和 `top/left: -9999px`）将其移出屏幕外。这样可以保持视频流的处理，同时在视觉上隐藏视频元素。

#### 3.4.2 视频流的权限管理

访问计算机摄像头需要用户的明确授权。浏览器通过 `MediaDevices.getUserMedia()` API 请求摄像头访问权限。当用户首次访问应用时，浏览器会显示一个权限请求对话框。

为了提高用户信任度，我们在应用启动时显示一个隐私通知模态框，明确告知用户：

1. 摄像头数据仅在浏览器本地处理。
2. 任何数据都不会上传到服务器。
3. 用户可以随时撤销摄像头访问权限。

### 3.5 数据采集与存储

#### 3.5.1 眼动数据格式

系统采集的眼动数据采用以下格式：

```typescript
interface GazeData {
  x: number;           // 屏幕上的 X 坐标（像素）
  y: number;           // 屏幕上的 Y 坐标（像素）
  timestamp: number;   // 时间戳（毫秒）
}
```

在追踪过程中，系统以约 30Hz 的频率采集眼动数据点，每秒约 30 个数据点。

#### 3.5.2 本地数据存储

所有眼动数据都存储在浏览器的内存中（使用 React 状态和 Ref），不涉及网络传输或服务器存储。用户可以选择导出数据或生成报告，但这些操作也都在浏览器本地完成。

#### 3.5.3 数据隐私保护

本工具采用以下措施保护用户隐私：

1. **本地处理**：所有数据处理均在浏览器中进行，不涉及服务器。
2. **无网络传输**：摄像头数据不通过网络传输。
3. **用户控制**：用户可以随时停止追踪、清除数据或撤销摄像头权限。
4. **透明性**：隐私政策清晰地说明了数据处理方式。

---

## 4. 数据可视化

### 4.1 热力图（Heatmap）

#### 4.1.1 热力图的原理

热力图是一种将眼动数据聚合并可视化的方法，用于展示用户在图像上不同区域的注意力分布。热力图中的颜色强度表示该区域的注意力强度：颜色越暖（红色），表示用户在该区域的注意力越高；颜色越冷（蓝色），表示注意力越低。

#### 4.1.2 Canvas 实现

本工具使用 HTML5 Canvas API 实现热力图的绘制，而不是依赖外部的 heatmap.js 库。这样做的优势包括：

1. **避免库的兼容性问题**：heatmap.js 库在某些情况下会产生 ImageData 只读属性的错误。
2. **性能优化**：原生 Canvas 实现通常比库的实现更快。
3. **灵活性**：可以根据需要自定义热力图的外观和行为。

#### 4.1.3 热力图绘制算法

热力图的绘制过程分为以下步骤：

**步骤 1：创建临时 Canvas**

创建一个临时的 Canvas 元素，用于绘制高斯模糊的热力数据。

```typescript
const tempCanvas = document.createElement('canvas');
tempCanvas.width = canvas.width;
tempCanvas.height = canvas.height;
const tempCtx = tempCanvas.getContext('2d');
```

**步骤 2：绘制径向梯度**

对于每个眼动数据点，在临时 Canvas 上绘制一个径向梯度，梯度的中心在数据点位置，向外逐渐衰减。

```typescript
data.forEach(point => {
  tempCtx.beginPath();
  const gradient = tempCtx.createRadialGradient(
    point.x, point.y, 0,
    point.x, point.y, radius
  );
  gradient.addColorStop(0, 'rgba(0, 0, 0, 1)');
  gradient.addColorStop(1, 'rgba(0, 0, 0, 0)');
  
  tempCtx.fillStyle = gradient;
  tempCtx.globalCompositeOperation = 'lighter';
  tempCtx.arc(point.x, point.y, radius, 0, Math.PI * 2);
  tempCtx.fill();
});
```

使用 `globalCompositeOperation = 'lighter'` 可以实现多个梯度的叠加效果，产生密度的累积。

**步骤 3：颜色映射**

将临时 Canvas 中的 Alpha 通道（表示密度）映射到颜色梯度。首先创建一个颜色梯度：

```typescript
const linearGradient = gradientCtx.createLinearGradient(0, 0, 256, 0);
linearGradient.addColorStop(0.0, 'rgba(0, 0, 255, 0)');      // 蓝色
linearGradient.addColorStop(0.3, 'rgba(0, 255, 0, 0.5)');    // 绿色
linearGradient.addColorStop(0.5, 'rgba(255, 255, 0, 0.7)');  // 黄色
linearGradient.addColorStop(1.0, 'rgba(255, 0, 0, 0.9)');    // 红色
```

然后，遍历临时 Canvas 的像素数据，将 Alpha 值映射到颜色梯度中的对应颜色。

**步骤 4：合成到主 Canvas**

将颜色映射后的图像数据放回主 Canvas，与背景图像合成。

```typescript
ctx.putImageData(imageData, 0, 0);
```

#### 4.1.4 热力图的参数调整

热力图的外观可以通过以下参数进行调整：

| 参数 | 说明 | 默认值 |
|-----|------|-------|
| radius | 每个数据点的影响半径（像素） | 40 |
| blur | 模糊程度 | 20 |
| colorStops | 颜色梯度的定义点 | 蓝→绿→黄→红 |

### 4.2 视线轨迹图（Gaze Plot）

#### 4.2.1 视线轨迹图的原理

视线轨迹图展示了用户眼动的时序过程，通过连接的线条和点来表示眼睛的运动路径。这种可视化方式可以揭示用户的阅读顺序和扫描模式。

#### 4.2.2 轨迹图的绘制

视线轨迹图的绘制过程如下：

**步骤 1：绘制连接线**

首先，绘制连接所有眼动数据点的线条，表示眼睛的运动轨迹。

```typescript
ctx.beginPath();
ctx.moveTo(data[0].x, data[0].y);

for (let i = 1; i < data.length; i++) {
  ctx.lineTo(data[i].x, data[i].y);
}

ctx.strokeStyle = 'rgba(37, 99, 235, 0.6)';
ctx.lineWidth = 3;
ctx.lineCap = 'round';
ctx.lineJoin = 'round';
ctx.stroke();
```

**步骤 2：绘制数据点**

在每个眼动数据点上绘制一个圆形，圆形的大小和透明度随时间增加而增加，表示时间的流逝。

```typescript
data.forEach((point, index) => {
  ctx.beginPath();
  const radius = 4 + (index / data.length) * 4;
  ctx.arc(point.x, point.y, radius, 0, 2 * Math.PI);
  
  const opacity = 0.4 + (index / data.length) * 0.6;
  ctx.fillStyle = `rgba(37, 99, 235, ${opacity})`;
  ctx.fill();
  
  ctx.strokeStyle = 'white';
  ctx.lineWidth = 1;
  ctx.stroke();
});
```

**步骤 3：标注关键点**

在视线轨迹图上标注关键数据点的序列号，帮助用户理解眼动的时序顺序。通常每 10 个点标注一次，同时标注起点和终点。

```typescript
if (index % 10 === 0 || index === 0 || index === data.length - 1) {
  ctx.fillStyle = 'white';
  ctx.font = '10px sans-serif';
  ctx.textAlign = 'center';
  ctx.textBaseline = 'middle';
  ctx.fillText(index.toString(), point.x, point.y);
}
```

#### 4.2.3 视线轨迹图的优势

相比热力图，视线轨迹图提供了以下额外的信息：

1. **时序信息**：用户可以看到眼睛运动的顺序和时间流向。
2. **扫描模式**：可以识别用户的阅读模式，例如从左到右、从上到下等。
3. **停留点**：通过点的密集程度可以识别用户停留较长时间的区域。

---

## 5. 系统工作流程

### 5.1 完整的用户交互流程

```
用户访问应用
    ↓
显示隐私通知 → 用户确认
    ↓
上传图像（拖拽或点击）
    ↓
进入校准阶段
    ├─ 显示校准说明
    ├─ 用户点击 9 个校准点（每点 3 次）
    └─ 系统收集校准数据
    ↓
进入追踪阶段
    ├─ 全屏显示上传的图像
    ├─ 自动开始眼动追踪
    └─ 用户阅读图像内容
    ↓
用户完成阅读（点击 X 按钮）
    ↓
生成数据报告
    ├─ 计算统计指标
    ├─ 生成热力图
    ├─ 生成视线轨迹图
    └─ 显示分析结果
    ↓
用户查看报告
    ├─ 切换热力图/视线轨迹图
    ├─ 导出数据
    └─ 返回首页或重新开始
```

### 5.2 状态管理

系统使用 React Hooks 进行状态管理，主要状态包括：

| 状态 | 类型 | 说明 |
|-----|------|------|
| isReady | boolean | WebGazer 是否初始化完成 |
| isTracking | boolean | 是否正在追踪眼动 |
| gazeData | GazeData \| null | 当前眼动数据点 |
| uploadedImage | string | 上传的图像 URL |
| gazeHistory | GazeData[] | 追踪过程中采集的所有眼动数据 |
| showReport | boolean | 是否显示报告页面 |

---

## 6. 性能优化

### 6.1 防止不必要的重新渲染

在开发过程中，我们发现高频率的眼动数据更新（约 30Hz）会导致 React 组件频繁重新渲染，从而引起倒计时卡顿等问题。解决方案包括：

#### 6.1.1 使用 useCallback 稳定函数引用

```typescript
const startTracking = useCallback(() => {
  gazeDataRef.current = [];
  setIsTracking(true);
}, []);

const stopTracking = useCallback(() => {
  setIsTracking(false);
  return gazeDataRef.current;
}, []);
```

通过 `useCallback` 确保函数引用在依赖项不变时保持稳定，避免不必要的组件重新渲染。

#### 6.1.2 使用 Ref 避免状态更新

对于需要在高频回调中访问的状态，使用 Ref 而不是 State：

```typescript
const gazeDataRef = useRef<GazeData[]>([]);

// 在高频回调中直接修改 Ref，不触发重新渲染
gazeDataRef.current.push(currentGaze);
```

#### 6.1.3 使用 hasStartedRef 防止倒计时重置

在 ImageViewer 组件中，使用一个 Ref 来标记是否已经启动追踪，防止父组件的重新渲染导致倒计时重置：

```typescript
const hasStartedRef = useRef(false);

useEffect(() => {
  if (!hasStartedRef.current) {
    hasStartedRef.current = true;
    onStartTracking();
  }
}, [onStartTracking]);
```

### 6.2 Canvas 性能优化

#### 6.2.1 离屏 Canvas

在热力图的绘制中，我们使用了离屏 Canvas（临时 Canvas）来进行中间计算，避免在主 Canvas 上进行复杂的绘制操作，从而提高性能。

#### 6.2.2 图像数据缓存

Canvas 的 `getImageData` 和 `putImageData` 操作相对昂贵，我们尽量减少这些操作的频率。

---

## 7. 已知限制与未来改进

### 7.1 当前限制

1. **准确度依赖于环境**：眼动追踪的准确度受到光线条件、摄像头质量、用户头部位置等多个因素的影响。

2. **个体差异**：不同用户的眼睛特征差异较大，某些用户可能需要更多的校准点以获得良好的追踪效果。

3. **头部运动敏感**：系统对用户头部运动较为敏感，用户需要保持相对稳定的头部位置。

4. **浏览器兼容性**：WebGazer.js 的性能在不同浏览器中可能有差异，某些旧版浏览器可能不支持。

### 7.2 未来改进方向

1. **头部姿态检测**：集成头部姿态检测功能，当用户头部移动幅度过大时自动暂停追踪并提示用户调整。

2. **区域兴趣分析（AOI）**：允许用户在图像上定义感兴趣的区域，自动计算这些区域的眼动统计指标。

3. **数据导出功能**：支持将眼动数据导出为 JSON、CSV 等格式，便于后续分析。

4. **数据平滑处理**：应用移动平均滤波器或其他平滑算法进一步减少眼动数据中的噪声。

5. **多图像测试**：支持在单个会话中上传和测试多张图像，生成对比性报告。

6. **移动设备适配**：优化校准算法以适应移动设备的摄像头和屏幕尺寸。

---

## 8. 结论

本文介绍了一个基于现代网络技术的眼动追踪工具的设计与实现。该工具充分利用了 WebGazer.js 库的功能，通过 React 框架和 Canvas API 实现了完整的眼动数据采集、校准、可视化和分析功能。系统采用了多项性能优化技术，确保了流畅的用户体验。通过隐私保护设计，系统在保护用户隐私的同时提供了强大的眼动追踪功能。

该工具具有广泛的应用前景，可以用于用户界面研究、教育研究、可用性测试等多个领域。未来的工作将重点关注提高追踪准确度、支持更复杂的分析功能，以及扩展对不同设备和环境的适配。

---

## 参考文献

[1] Papoutsaki, A., Sangkloy, P., Laskey, J., Daskalova, N., Huang, J., & Hays, J. (2016). WebGazer: Scalable Webcam Eye Tracking Using User Interactions. In *Proceedings of the 25th International Joint Conference on Artificial Intelligence (IJCAI)*.  
https://cs.brown.edu/people/apapouts/papers/ijcai2016webgazer.pdf

[2] WebGazer.js Official Website. Brown University HCI Group.  
https://webgazer.cs.brown.edu/

[3] Tobii Pro. Eye Tracking in UX Research.  
https://www.tobiipro.com/

[4] Blascheck, T., Kurzhals, K., Raschke, M., Burch, M., Weiskopf, D., & Ertl, T. (2017). Visualization of Eye Tracking Data: A Taxonomy and Survey. *Computer Graphics Forum*, 36(8), 260-284.

[5] Heatmap Visualizations from Signifier Eyetracking Experiment. Nielsen Norman Group.  
https://www.nngroup.com/articles/heatmap-visualizations-signifiers/

[6] Špakov, O., & Miniotas, D. (2007). Visualization of Eye Gaze Data Using Heat Maps. In *Proceedings of the 2nd Conference on Communication by Gaze Interaction (COGAIN)*.

[7] Menges, R., Kramer, S., Hill, M., Nisslmueller, T., & Kumar, C. (2020). A Visualization Tool for Eye Tracking Data Analysis in the Web. In *Proceedings of the 2020 ACM Symposium on Eye Tracking Research and Applications*.

[8] MediaDevices.getUserMedia() - MDN Web Docs.  
https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getUserMedia

[9] Canvas API - MDN Web Docs.  
https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API

[10] React Hooks Documentation.  
https://react.dev/reference/react/hooks

---

**文档版本历史**

| 版本 | 日期 | 变更 |
|-----|------|------|
| 1.0 | 2026-02-05 | 初始版本 |

---

**作者信息**

本文档由 Manus AI 编写，基于眼动追踪工具的完整实现代码和设计文档。

**许可证**

本文档采用 Creative Commons Attribution 4.0 International License 许可证。

**联系方式**

如有任何问题或建议，欢迎通过以下方式联系：

- 项目 GitHub 仓库：https://github.com/manus-ai/eye-tracking-tool
- 官方网站：https://manus.im

