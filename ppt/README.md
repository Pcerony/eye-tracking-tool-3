# Legacy Deck Baseline

This directory is a rollback and migration input, not the active presentation
source. `index.html` and `项目记录.md` may contain pre-existing work and must not
be normalized, regenerated, or staged during unrelated maintenance.

Active work lives under `src/`; the release artifact is `dist/index.html`.
Historical images remain here only because migration checks resolve their
legacy paths. Derived source inventories moved to `materials/derived/`.
