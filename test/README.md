# Legacy Test Area

Current automated QA lives under `tests/` and runs through npm scripts.

Files in this directory predate the modular build. Some are user-modified or
untracked and are intentionally left in place. Do not execute bulk update
scripts, add new tests here, or stage these files as part of unrelated work.
